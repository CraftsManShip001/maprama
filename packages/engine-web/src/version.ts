/** Engine name reported in the `ready` event. */
export const ENGINE_NAME = '@diorama/engine-web';
/** Engine version reported in the `ready` event (kept in sync with package.json). */
export const ENGINE_VERSION = '0.0.0';
